Once you are in the runtime workbench, create a new project (a normal one)

inside, create a new pnml document, a geometry document and a Config document.

open the Config and in the root of the tree right clicking select "Load Resource"
and select the Petri net and then repeating the process, the geometry.

Then select the Config object and in the properties view select the Petri net and the 
geometry.

Then right click on the Config object, select runCommand and run